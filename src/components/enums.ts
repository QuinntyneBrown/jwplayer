export enum playlistState {
    LOADED,
    NOT_LOADED
}

export enum jwPlayerState {
    PLAY,
    PAUSE,
    BUFFER,
    IDLE
}

export const keys = {
    WATCH_HISTORY_INDEX: "WATCH_HISTORY_INDEX",
    WATCH_HISTORY_POSITION: "WATCH_HISTORY_POSITION",
    WATCH_HISTORY_MEDIA_ID: "WATCH_HISTORY_MEDIA_ID",
}