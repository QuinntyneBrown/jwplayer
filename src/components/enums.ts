export enum playlistState {
    LOADED,
    NOT_LOADED
}

export enum jwPlayerState {
    PLAY,
    PAUSE,
    BUFFER,
    IDLE
}

export const keys = {
    WATCH_HISTORY_INDEX: "PLAYLIST_INDEX",
    WATCH_HISTORY_POSITION: "POSITION",
    WATCH_HISTORY_MEDIA_ID: "CURRENT_MEDIA_ID",
}